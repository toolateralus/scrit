import * from raylib


func() {
  initWindow(800,600, "game")
  
  for {
    if windowShouldClose() {
      break
    }
    clearBackground()
    beginDrawing()
    
    drawText(250, 250, 42, tostr({x = 0, y = 0}), colors.WHITE)
    
    drawFPS(0,0)
    endDrawing()
  }
}()
