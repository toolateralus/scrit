import * from raylib

func() {
  initWindow(800,600, "game")

  for {
    if windowShouldClose() {
      break
    }
    clearBackground()
    beginDrawing()
    
    drawText(250, 250, 24, "My Text", colors.WHITE)
    
    drawFPS(0,0)
    endDrawing()
  }
}()
